# import torch
# from core.logic.yolovface.detectface import detect_one

# def detector(image):

    
#     device = "cpu"#torch.device("cuda" if torch.cuda.is_available() else "cpu")
    
#     #model = load_model("core/logic/yolovface/weights/models.pt", device)

#     return detect_one(image, device)

